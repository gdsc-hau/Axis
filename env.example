# Operator-only tooling environment. Application runtimes use their own
# apps/*/.env.example files.
# Obtain the complete connection string through the approved operator channel.
# Leave this blank in the tracked template so credential scanners never mistake
# an example URL for a committed database password.
DATABASE_URL=
SUPABASE_PROJECT_REF=your-project-ref
