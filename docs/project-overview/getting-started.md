# Getting Started

1. Install dependencies with `pnpm install`.
2. Configure local environment variables from `env.example`.
3. Start the app with `pnpm dev`.

For documentation work, the MkDocs site can be served separately with `mkdocs serve` once MkDocs is installed in your environment.
